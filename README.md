# 📝 نظام إنشاء وتصحيح الاختبارات الآلي (OMR)

هذا المشروع يهدف إلى أتمتة عملية إنشاء أوراق أسئلة وإجابات قابلة للقراءة الآلية **(OMR)**، بالإضافة إلى معالجة وتصحيح أوراق إجابات الطلاب باستخدام تقنيات معالجة الصور.

## 🌟 الميزات الرئيسية

* **إنشاء اختبارات مخصصة:** توليد صور ورقة الأسئلة وصور ورقة الإجابة (Bubble Sheet) لكل طالب بناءً على ملف بيانات JSON مركزي.
* **دعم النصوص العربية:** التعامل السليم مع النصوص العربية المعقدة (مثل التشكيل والعرض ثنائي الاتجاه) لضمان طباعة سليمة للاختبارات.
* **رموز QR للتعقب:** تضمين **رمز QR** في كل ورقة لتشفير بيانات الطالب والامتحان لتسهيل التعقب الآلي وضمان فرز دقيق.
* **تصحيح آلي بالرؤية الحاسوبية:** تحليل صور أوراق الإجابة الممسوحة ضوئيًا باستخدام مكتبة **OpenCV** للكشف عن الإجابات المظللة وتصحيحها آليًا بناءً على مفتاح الإجابة.
* **إخراج النتائج:** حفظ النتائج المصححة (مفتاح الإجابة، إجابات الطالب، النتيجة النهائية) في ملف **JSON** منظم.

***

## 🛠️ المتطلبات الأساسية

يتطلب المشروع تثبيت بيئة Python بالإضافة إلى المكتبات التالية:

```bash
pip install Pillow qrcode arabic-reshaper python-bidi opencv-python numpy
🚀 كيفية الاستخدام
المرحلة الأولى: إنشاء المستندات
استخدم generate_exams.py لتوليد صور الاختبارات وأوراق الإجابة لكل طالب.

التأكد من البيانات: يجب أن يكون ملف jsonQ.json مُعداً بجميع بيانات الاختبار والطلاب.

التنفيذ:

Bash

python generate_exams.py

المرحلة الثانية: التصحيح الآلي (OMR Scanning)
بعد طباعة الأوراق وحصول الطلاب على الاختبار، استخدم omr_scanner.py لمعالجة أوراق الإجابة الممسوحة ضوئيًا.

إعداد الصورة: ضع صورة ورقة إجابة الطالب الممسوحة ضوئيًا (بجودة عالية ووضوح) في مسار يمكن الوصول إليه.

التنفيذ:

Bash

python omr_scanner.py
ملاحظة هامة: قد تحتاج إلى تعديل مسار ملف الصورة المدخل (Input Image Path) ومسار ملف بيانات الفقاعات (Bubble Data JSON Path) داخل سكربت omr_scanner.py ليناسب ملفاتك.

الناتج: يتم حفظ ملف JSON يحتوي على نتائج التصحيح، بالإضافة إلى حفظ صورة معالجة تُظهر التعرف على الإجابات للتأكيد البصري.

⚙️ ملاحظات تقنية
الخطوط (Fonts): يعتمد السكربت على ملف خط NotoKufiArabic-Regular.ttf. يجب وضعه في نفس مسار تشغيل السكربت لتجنب أخطاء الخطوط عند التعامل مع النصوص العربية.

أبعاد الصفحة: تم ضبط أبعاد الصورة على 1240x1754 بكسل، وهي أبعاد مناسبة لورقة A4 بجودة طباعة متوسطة.

معايرة التصحيح: يعتمد سكربت omr_scanner.py على قيمة عتبة (Threshold) لتمييز التظليل. إذا كانت نتائج التصحيح غير دقيقة بسبب اختلاف جودة المسح الضوئي، قد ت
